# Kali Security Environment Audit — ashborn

**Generated:** 2026-04-15 00:24 UTC
**Host:** ashborn
**Operator:** dxverm

---

## 1. Executive Summary

System is healthy, freshly booted (9 min uptime), and running current Kali rolling (2026.1) on kernel 6.18.12+kali-amd64. All core Kali metapackages are in sync at 2026.1.8. Disk at 26% use, 18 GiB RAM free, 24 GiB swap untouched — ample headroom.

**Tool coverage score: 26/26 spot-checked** — every Tier-1 tool in the quick check is present. Broader inventory shows ~3850 installed packages plus `kali-linux-default` coverage. GPU cracking capability is online via rusticl/Mesa on the AMD WX 3200.

**Top 3 gaps / watch items:**
1. **Exposed Ollama (`*:11434`)** — LLM API listening on all interfaces (pid 1361). Reachable on wlan0 (192.168.0.100) and Tailscale. No auth. Restrict to 127.0.0.1 or Tailscale-only.
2. **Firewall effectively open** — INPUT policy is ACCEPT with only the `ts-input` Tailscale chain attached. No UFW/nftables baseline for non-Tailscale traffic on a mobile Kali host.
3. **Taranis ingress exposed on LAN (`0.0.0.0:8081`)** — `taranis-ingress-1` container binds publicly (not loopback like the rest of the stack). If not intentional, constrain the port binding in its compose file to `127.0.0.1:8081` or a Tailscale address.

---

## 2. System Profile

| Component | Detail |
|---|---|
| Distro | Kali GNU/Linux Rolling 2026.1 (kali-rolling) |
| Kernel | Linux 6.18.12+kali-amd64 (built 2026-02-25) |
| CPU | Intel Core i7-8565U @ 1.80 GHz, 4c/8t, max 4.6 GHz |
| RAM | 30 GiB total, 12 GiB used, 18 GiB avail |
| Swap | 24 GiB (0 used) |
| Disk | 477 GB NVMe → LVM: `/` 443 GB (26% used), `/boot` 944 MB, EFI 975 MB |
| GPU (iGPU) | Intel UHD Graphics 620 (WhiskeyLake-U) |
| GPU (dGPU) | AMD Radeon PRO WX 3200 (Lexa XT / polaris12) |
| Virtualization | Bare metal (none) |
| Uptime | 9 min (booted 2026-04-15 00:15) |

**CPU mitigations:** All current Intel speculative-exec mitigations applied (Retbleed, Spectre v1/v2, SSB, MDS, Vmscape, TSA). Only residual note: MMIO stale data — SMT vulnerable (expected on WhiskeyLake with HT enabled).

---

## 3. Network Posture

### Interfaces
| Interface | State | Address | Notes |
|---|---|---|---|
| lo | UP | 127.0.0.1/8 | — |
| eth0 | DOWN (no carrier) | — | wired NIC unused |
| wlan0 | UP | 192.168.0.100/24 + IPv6 | primary uplink, DHCP |
| tailscale0 | UP | 100.70.118.16/32, fd7a:115c:a1e0::d632:7610 | Tailscale mesh |
| docker0 | DOWN | 172.17.0.1/16 | default bridge, no active ctrs |
| br-969a11966562 | UP | 172.19.0.1/16 | user-defined |
| br-ad4247503754 | UP | 172.20.0.1/16 | user-defined |
| br-148f4fd236e2 | UP | 172.18.0.1/16 | user-defined |
| br-7053fd7911d5 | UP | 172.21.0.1/16 | user-defined (most veths here) |

17 `veth*` pairs attached — active container workload spread across the 4 custom bridges.

### Listening ports
| Bind | Port | Service |
|---|---|---|
| `0.0.0.0:41641` udp | Tailscale WireGuard endpoint |
| `127.0.0.1:5432` | `pgvector` container (Pentagi stack) via docker-proxy |
| `127.0.0.1:8443` | `pentagi` container (Pentagi UI/API) via docker-proxy |
| `127.0.0.1:9443` | `scraper` container (Pentagi) via docker-proxy |
| `127.0.0.1:9187` | `pgexporter` (Prometheus postgres_exporter) |
| `127.0.0.1:27947` | **containerd local control socket** (benign) |
| `100.70.118.16:58996` / `[fd7a:…]:42744` | `tailscaled` (mesh node ports) |
| **`0.0.0.0:8081`** | **`taranis-ingress-1` — LAN-exposed Taranis AI ingress** |
| **`*:11434`** | **`ollama` (pid 1361) — all interfaces, no auth** |

### Firewall
iptables INPUT policy = **ACCEPT**. Only chain installed is Tailscale's `ts-input`. FORWARD = DROP (Docker manages its own chains elsewhere). **No host-level ingress filter** — recommend establishing an nftables baseline that only allows established/related + Tailscale + explicit exceptions.

---

## 4. Tool Inventory

All Tier-1 tools verified present.

| Category | Tool | Status |
|---|---|---|
| Port/Network | nmap | ✅ |
| Port/Network | masscan | ✅ |
| Port/Network | rustscan | ✅ |
| Recon | amass | ✅ |
| Recon | subfinder | ✅ |
| Recon | httpx | ✅ |
| Web | nuclei | ✅ |
| Web | feroxbuster | ✅ |
| Web | gobuster | ✅ |
| Web | ffuf | ✅ |
| Web | sqlmap | ✅ |
| Exploitation | msfconsole (Metasploit) | ✅ |
| AD | netexec | ✅ |
| AD | nxc | ✅ |
| AD | kerbrute | ✅ |
| Password | hashcat | ✅ |
| Password | john | ✅ |
| Password | hydra | ✅ |
| Wireless | aircrack-ng | ✅ |
| Wireless | wifite | ✅ |
| Traffic | wireshark | ✅ |
| Tunneling | chisel | ✅ |
| Tunneling | ligolo-proxy | ✅ |
| Forensics | volatility (vol) | ✅ |
| RE | ghidra | ✅ |
| RE | radare2 | ✅ |
| Post-ex | pwncat-cs | ✅ |

**Installed packages:** 3850. Kali metas `kali-linux-core`, `kali-linux-default`, `kali-linux-firmware`, `kali-linux-headless` all at 2026.1.8.

---

## 5. Wordlist Status

**Present:**
- `/usr/share/seclists/` — full SecLists (Discovery, Fuzzing, Passwords, Payloads, Usernames, Web-Shells, Pattern-Matching, Miscellaneous)
- `/usr/share/wordlists/` — `dirb`, `dirbuster`, `dnsmap.txt`, `fasttrack.txt`, `fern-wifi`, `john.lst`, `legion`, `metasploit`

**Notable absent (verify):**
- `rockyou.txt` not in listing — usually symlinked. Check `/usr/share/wordlists/rockyou.txt` and `.gz`.
- No `assetnote` commonspeak2 or `Probable-Wordlists` visible as top-level dirs.

**Recommendation:** `sudo gunzip /usr/share/wordlists/rockyou.txt.gz` if gzipped; optionally pull assetnote wordlists for modern web fuzzing.

---

## 6. GPU / OpenCL Cracking Capability

- **Platform:** rusticl (Mesa CL — default in Kali 2026.1 era)
- **Device:** AMD Radeon PRO WX 3200 (polaris12), DRM 3.64
- **Expected hashcat perf:** ~2 GH/s MD5 (per stored profile), adequate for NetNTLMv2, Kerberos AS-REP/Kerberoast, and WPA handshakes; slow for bcrypt/scrypt/Argon2.
- **Status:** functional — `clinfo` shows the device exposed across all platform slots.

No NVIDIA / CUDA path on this host. For heavy bcrypt/scrypt work, offload to a dedicated cracking rig.

---

## 7. Recommendations (prioritized)

### P0 — Security hygiene
1. **Lock down Ollama:**
   ```bash
   sudo systemctl edit ollama.service
   # [Service]
   # Environment="OLLAMA_HOST=127.0.0.1:11434"
   sudo systemctl restart ollama
   ```
2. **Constrain Taranis ingress (`0.0.0.0:8081` → `taranis-ingress-1`)** to loopback or Tailscale in its compose port mapping unless LAN exposure is required.
3. ~~`127.0.0.1:27947`~~ — **resolved:** containerd local control socket (pid 1132). Benign.
4. **Establish nftables baseline:**
   ```bash
   sudo apt install -y nftables
   sudo systemctl enable --now nftables
   # default: drop inbound, allow lo + established + tailscale0
   ```

### P1 — Lab hygiene
5. **Docker state is NOT stale** — `docker ps` shows two active stacks:
   - **Pentagi** (vxcontrol): `pentagi`, `scraper`, `pgvector`, `pgexporter` — all bound to 127.0.0.1. Healthy.
   - **Taranis AI** (ghcr.io/taranis-ai): `ingress`, `core`, `frontend`, `database`, `redis`, `sse`, `cron`, `workers`, `collector`, `nlp_bot`, `summary_bot`, `story_bot` + local `taranis-llm-enricher`. Mostly healthy; `taranis-story_bot-1` unhealthy, `taranis-core-1` still starting.
   - `taranis-ingress-1` is the only container binding `0.0.0.0`. Intentional for LAN access? If not, edit its compose port mapping to `127.0.0.1:8081:8080`.
6. **rockyou decompressed** → `/usr/share/wordlists/rockyou.txt` (134M). `.gz` (51M) kept as backup.

### P2 — Capability improvements
7. Consider installing `assetnote-wordlists` for modern content discovery.
8. Baseline a Lynis + debsecan scan (`debian-harden` skill) now that the system is fresh-booted.
9. Snapshot this audit into `/home/dxverm/.claude/projects/-home-dxverm/memory/project_kali_setup.md` index if the hardware/software fingerprint has drifted from the stored profile.

---

## 8. Services

25 active units. Expected Kali desktop + lab stack:

**Core OS:** systemd-journald, systemd-logind, systemd-timesyncd, systemd-udevd, systemd-userdbd, dbus, polkit, cron, haveged, accounts-daemon, udisks2, upower, smartmontools, rtkit-daemon, bolt (Thunderbolt), tlp-pd (power), user@1000.

**Desktop/Net:** sddm (KDE login), NetworkManager, wpa_supplicant, ModemManager.

**Lab stack:**
- `docker.service` + `containerd.service` — container runtime
- `tailscaled.service` — mesh VPN
- `ollama.service` — local LLM (⚠ exposed, see P0)

**Nothing unexpected** in the active unit list. PostgreSQL is listening but no `postgresql.service` appears in the active list snippet — it may be running under a docker container (likely, given the postgres_exporter on 9187 and bridge networks). Confirm with `ss -lptn 'sport = :5432'`.

---

## Index (for future sessions)

- Host: **ashborn** — Kali rolling 2026.1, kernel 6.18.12+kali
- Hardware: i7-8565U / 30 GiB RAM / 477 GB NVMe / AMD WX 3200 (rusticl OpenCL)
- Network: wlan0 `192.168.0.100/24`, tailscale0 `100.70.118.16`, 4 docker custom bridges
- Listeners of note: Ollama `*:11434` (exposed), `0.0.0.0:8081`, PG `127.0.0.1:5432`, TLS apps on `127.0.0.1:8443/9443`
- Toolset: kali-linux-default @ 2026.1.8, 3850 pkgs, all Tier-1 tools present
- Cracking: hashcat on AMD WX 3200 via rusticl (~2 GH/s MD5 class)
- Firewall: **none beyond Tailscale** — establish nftables baseline
- Prior reports: `~/kali-enhancement-project/01-system-audit-report.md`, `system-deep-audit-2026.md`, `06-final-tool-inventory.md`
